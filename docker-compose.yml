services:
  foto:
    image: 10.0.0.58:5002/foto:1.0.1
    container_name: foto
    ports:
      - "8021:8021"
    volumes:
      - ./data:/data
    restart: unless-stopped
    environment:
      - FOTO_USERNAME=admin
      - FOTO_PASSWORD=changeme
